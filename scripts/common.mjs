import { API_BASE_URL, registerUser } from "./pages/registration.mjs";
import { loginUser, loginUrl, userToLogin } from "./pages/login.mjs";
import { getWithToken } from "./auth/token.mjs";

// Log in a user
// loginUser(loginUrl, userToLogin);

getWithToken(getWithToken);
